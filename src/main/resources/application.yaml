server:
  port: 8888

spring:
  application:
    name: lesson60lab

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 8803330605
    driver-class-name: org.postgresql.Driver

  mail:
    host: smtp.gmail.com
    port: 587
    username: dadep8572@gmail.com
    password: fbppgogzbwnkasxs
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    enabled: false

  freemarker:
    template-loader-path: classpath:/templates/
    suffix: .ftlh

  mvc:
    static-path-pattern: /static/**

  web:
    resources:
      static-locations:
        - classpath:/static/
        - file:src/main/resources/static/

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB